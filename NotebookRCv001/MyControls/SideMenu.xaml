<UserControl x:Class="NotebookRCv001.MyControls.SideMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:NotebookRCv001.ViewModels"
      xmlns:local="clr-namespace:NotebookRCv001.MyControls"
      mc:Ignorable="d">
    <!--<UserControl.Resources>
        <ResourceDictionary>
            <HierarchicalDataTemplate x:Key="MenuItemTemplate" ItemsSource="{Binding SubItems}">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="{Binding Icon}" FontSize="16" Foreground="White" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </HierarchicalDataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>-->
    <Grid>
        <TreeView x:Name="sidemenu" Width="100" Height="200">
            
        </TreeView>

        <!--<ListView x:Name="SideMenu" Background="#333" Width="200" SelectionChanged="SideMenu_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="{Binding Icon}" FontSize="16" Foreground="White" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>-->
    </Grid>
</UserControl>
